#include<stdio.h.>

int main(){
    int age ;
    printf("Enter age (in years ) :");
    scanf("%d",&age);
    if(age>=18)
    printf("Eligible for voting .");
    if(age<18)
    printf("Not eligible for voting .");



}