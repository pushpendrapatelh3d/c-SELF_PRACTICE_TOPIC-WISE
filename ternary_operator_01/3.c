#include<stdio.h>

int main(){
    int a,b;
    printf("Enter a number(a) :");
    scanf("%d",&a);
    printf("Enter a number(b) :");
    scanf("%d",&b);
    a>b
    ?printf("a is greater than b")
    :printf("b is gtrater than a");
}